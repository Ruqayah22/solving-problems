/*
D. Same values
time limit per test2 seconds
memory limit per test256 megabytes
inputstandard input
outputstandard output
You are given an array of n integer numbers. For each position in the array find a distance to the closest elements to the left with the same value. If there's no such element, output  - 1.

Input
The first lien contains a single integer number n — the size of the array (1 ≤ n ≤ 200 000).

The second line contains n integer numbers — the array. All numbers don't exceed 109 by absolute value.

Output
Output n numbers — answers for all the positions in the array.

Example
inputCopy
5
1 1 2 1 2
outputCopy
-1 1 -1 2 2 
*/

